java -jar RandPublish1.jar

The client can be compiled and executed straightforwardly. 
After the RandService is published, WSDL & XML Schema are generated by publisher.

On the client side, WSDL can be requested:
curl http://localhost:8888/rs?wsdl
On the client side, XML Schema can be requested:
curl http://localhost:8888/rs?xsd=1
